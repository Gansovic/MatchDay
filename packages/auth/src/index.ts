/**
 * @matchday/auth
 *
 * Shared authentication utilities for MatchDay monorepo
 *
 * Note: validator.ts, role.service.ts, and api-auth.ts are app-specific
 * and remain in each app's src/lib/auth/ directory
 */

export * from './auth.service';
